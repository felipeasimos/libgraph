name: Tests

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: valgrind install
      run: sudo apt install valgrind
    - name: make
      run: make DEBUGGER_COMMAND=
